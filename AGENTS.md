プログラミングコード作成時：

- まずは詳細なコーディングのプランを立てる
  - プランを立て終わったら、一旦ユーザーにターンを回す
  - 標準モジュール以外を用いるときは、プランを立てる段階で書き出す
- プランに基づいて、プログラムをコーディングする
  - ライブラリについて少しでもわからないことがあれば、skillsのcontext7を使用して調べる
  - コードはRuffのlintに準拠する（`pyproject.toml`を参考にする）
  - 基本的にはPythonの標準モジュールを使用する
- コードが完成したら
  - Ruffのエラーを確認する
  - Pythonファイルを実行するときは`uv run --env-file .env ファイル名`を使用する
