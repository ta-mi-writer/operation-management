プログラミングコード作成時：

- パッケージのインストールを行わない。インストールは私に依頼するか、セッションを閉じたら自動で消えるような方法にする
- まずは詳細なコーディングのプランを立てる
  - プランを立て終わったら、一旦ユーザーにターンを回す
  - 標準モジュール以外を用いるときは、プランを立てる段階で書き出す
- プランに基づいて、プログラムをコーディングする
  - この環境には`uv`が使用可能
    - `pyhon`コマンドや`pip`コマンドは使用せず、`uv`コマンドを使用する
  - コードはRuffのlintに準拠する（`pyproject.toml`を参考にする）
    - `uv run ruff check ファイル名`コマンドを使用してエラーを確認する
  - 基本的にはPythonの標準モジュールを使用する
- コードが完成したら
  - Ruffのエラーを確認する
  - 環境変数を用いたPythonファイルを実行するときは`uv run --env-file .env ファイル名 その他の引数`を使用する
